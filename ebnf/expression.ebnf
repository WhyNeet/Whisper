expression = term ;
term = factor { ( "+" | "-" ) factor } ;
factor = unary { ( "* | "/" ) unary } ;
unary = ( "!" | "-" ) primary | primary ;
primary = IDENTIFIER | NUMBER | grouping | block | "true" | "false" ;
grouping = "(" expression ")" ;
block = "{" { statement } "}" ;
